#!/command/with-contenv bashio

bashio::log.info "Starting YOLO Print Detection..."

export CAMERA_URL="$(bashio::config 'camera_url')"
export THRESHOLD="$(bashio::config 'threshold')"

cd /app

exec python3 detector.py
