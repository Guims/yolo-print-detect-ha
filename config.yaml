name: YOLO Print Detector
slug: yolo_print_detect_ha
version: "1.0.0"

startup: services
boot: auto
init: false

arch:
  - aarch64
  - amd64

ports:
  5001/tcp: 5001

webui: "http://[HOST]:[PORT:5001]"

build: .
